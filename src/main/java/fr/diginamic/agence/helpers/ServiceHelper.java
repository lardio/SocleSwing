package fr.diginamic.agence.helpers;

public class ServiceHelper {
    /*
    public static VehiculeService HelperVehiculeService(Vehicule vehicule) {
        VehiculeService vehiculeService;
        if(vehicule.getNom().equals(Voiture.class.getSimpleName())) {
            vehiculeService = (VoitureService)  new VoitureService();
        } else {
            vehiculeService = (VoitureService) new VoitureService();
        }
        return vehiculeService;
    }
    */


}
